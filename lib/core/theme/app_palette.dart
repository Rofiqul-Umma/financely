import 'package:flutter/material.dart';

/// Selectable "Material You" accent seeds shown in Settings.
class AppPalette {
  const AppPalette._();

  static const Color defaultSeed = Color(0xFFB4004E);

  static const List<Color> seeds = <Color>[
    Color(0xFFB4004E), // Pink (brand default)
    Color(0xFF386A20), // Green
    Color(0xFF0061A4), // Blue
    Color(0xFFB3261E), // Red
    Color(0xFF7D5260), // Rose
    Color(0xFF8B5000), // Amber
    Color(0xFF00696D), // Teal
    Color(0xFF565992), // Indigo
  ];
}
